export interface IPagination {
  limit: number;
  offset: number;
}

export interface ICountry {
  name: string;
  code: string;
  flag: string;
  latitude: number;
  longitude: number;
}
